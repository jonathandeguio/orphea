apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    name: bosler-spark-history-server
  name: bosler-spark-history-server
  namespace: bosler
spec:
  replicas: 1
  selector:
    matchLabels:
      name: bosler-spark-history-server
  template:
    metadata:
      labels:
        name: bosler-spark-history-server
    spec:
      containers:
        - name: bosler-spark-history-server
          image: europe-west6-docker.pkg.dev/bosler-dev2/bosler-cr/spark-history-server:latest

          resources:
            requests:
              memory: "512Mi"
              cpu: "100m"

          command:
            - /usr/bin/tini
            - -s
            - --
            - /opt/spark/bin/spark-class
            - -Dspark.history.fs.logDirectory=s3a://spark-streaming/checkpoint/
            - org.apache.spark.deploy.history.HistoryServer

          ports:
            - name: http
              protocol: TCP
              containerPort: 18080

          readinessProbe:
            timeoutSeconds: 4
            httpGet:
              path: /
              port: http

          livenessProbe:
            timeoutSeconds: 4
            httpGet:
              path: /
              port: http

---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: bosler-spark-history-server
  name: bosler-spark-history-server
  namespace: bosler
spec:
  type: LoadBalancer
  ports:
    - name: bosler-spark-history-server
      port: 18080
      targetPort: 18080
  selector:
    name: bosler-spark-history-server
status:
  loadBalancer: {}
