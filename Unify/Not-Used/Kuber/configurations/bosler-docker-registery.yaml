apiVersion: apps/v1
kind: Deployment
metadata:
  name: bosler-docker-registry
  namespace: bosler
  labels:
    app: bosler-docker-registry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bosler-docker-registry
  template:
    metadata:
      labels:
        app: bosler-docker-registry
    spec:
      volumes:
        - name: certs-vol
          hostPath:
            path: /bosler/docker/certs/
            type: Directory
        - name: registry-vol
          hostPath:
            path: /bosler/docker/registry/
            type: Directory

      containers:
        - image: registry:2
          name: bosler-docker-registry
          imagePullPolicy: IfNotPresent
          env:
            - name: REGISTRY_HTTP_TLS_CERTIFICATE
              value: "/bosler/docker/certs/registry.crt"
            - name: REGISTRY_HTTP_TLS_KEY
              value: "/bosler/docker/certs/registry.key"
          ports:
            - containerPort: 5000
          volumeMounts:
            - name: certs-vol
              mountPath: /bosler/docker/certs/
            - name: registry-vol
              mountPath: /var/lib/registry/

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: bosler-docker-registry
  name: bosler-docker-registry
  namespace: bosler
spec:
  ports:
    - port: 5000
      nodePort: 31320
      protocol: TCP
      targetPort: 5000
  selector:
    app: bosler-docker-registry
  type: NodePort
