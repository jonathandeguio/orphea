options:
  logging: CLOUD_LOGGING_ONLY
steps:
  # Step 1
  #  - name: 'gcr.io/cloud-builders/docker'
  #    entrypoint: 'bash'
  #    args: [
  #        '-c',
  #        'docker pull europe-west1-docker.pkg.dev/$PROJECT_ID/movetodata-cr/boson:latest || exit 0'
  #    ]
  # Step 2
  - name: gcr.io/cloud-builders/docker
    args: [
        'build',
        '-t',
        'europe-west1-docker.pkg.dev/$PROJECT_ID/movetodata-cr/boson:$TAG_NAME',
        '.'
    ]

  - name: gcr.io/cloud-builders/docker
    args: [
        'push',
        'europe-west1-docker.pkg.dev/$PROJECT_ID/movetodata-cr/boson:$TAG_NAME'
    ]
    # Step 4
    #  - name: 'gcr.io/cloud-builders/kubectl'
    #    args: [
    #        'set',
    #        'image',
    #        'deployment',
    #        'boson',
    #        'boson=europe-west1-docker.pkg.dev/$PROJECT_ID/movetodata-cr/boson:latest',
    #        '--namespace',
    #        'movetodata'
    #    ]
    env:
      - 'CLOUDSDK_COMPUTE_ZONE=europe-west1-b'
      - 'CLOUDSDK_CONTAINER_CLUSTER=movetodata'
images: [
    'europe-west1-docker.pkg.dev/$PROJECT_ID/movetodata-cr/boson:$TAG_NAME'
]
timeout: 2400s

