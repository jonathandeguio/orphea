# =============================================================================
# MoveToData Platform — Stack principale (Boson + Frontend + PostgreSQL + Redis)
# Usage : docker compose -f docker-compose.core.yml --env-file .env.movetodata up -d
# =============================================================================

services:

  boson-db:
    image: postgres:14-alpine
    container_name: movetodata-boson-db
    environment:
      POSTGRES_DB: ${BOSON_DB_NAME:-boson}
      POSTGRES_USER: ${BOSON_DB_USERNAME:-movetodata}
      POSTGRES_PASSWORD: ${BOSON_DB_PASSWORD}
    volumes:
      - ${MOVETODATA_MOUNT_PATH:-/movetodata}/postgres/boson:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${BOSON_DB_USERNAME:-movetodata} -d ${BOSON_DB_NAME:-boson}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - movetodata-net
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  movetodata-redis:
    image: redis:7-alpine
    container_name: movetodata-redis
    command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - ${MOVETODATA_MOUNT_PATH:-/movetodata}/redis:/data
    ports:
      - "127.0.0.1:6379:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - movetodata-net
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"

  boson:
    image: movetodata/boson:latest
    container_name: movetodata-boson
    build:
      context: ../boson
      dockerfile: Dockerfile
    command:
      - /bin/sh
      - -lc
      - >
        JAR=$$(find /app -maxdepth 1 -name '*.jar' ! -name '*-plain.jar' | head -n 1);
        test -n "$$JAR";
        exec java $$JAVA_OPTS -jar "$$JAR"
    depends_on:
      boson-db:
        condition: service_healthy
      movetodata-redis:
        condition: service_healthy
    environment:
      DB_USERNAME: ${BOSON_DB_USERNAME:-movetodata}
      DB_PASSWORD: ${BOSON_DB_PASSWORD}
      DB_HOST: boson-db
      DB_PORT: 5432
      TOKEN_SECRET: ${TOKEN_SECRET}
      TOKEN_EXPIRATION: ${TOKEN_EXPIRATION:-604800000}
      BASE_URL: ${BASE_URL:-http://localhost:8080}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:3000,http://localhost:8080}
      MOVETODATA_MOUNT_PATH: ${MOVETODATA_MOUNT_PATH:-/movetodata}
      SAML2_SSO_CONFIG: ${SAML2_SSO_CONFIG:-file:/etc/movetodata/saml.yml}
      GITHUB_OAUTH_CLIENT_ID: ${GITHUB_OAUTH_CLIENT_ID:-}
      GITHUB_OAUTH_CLIENT_SECRET: ${GITHUB_OAUTH_CLIENT_SECRET:-}
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID:-}
      GOOGLE_OAUTH_CLIENT_SECRET: ${GOOGLE_OAUTH_CLIENT_SECRET:-}
      SWAGGER_UI: ${SWAGGER_UI:-false}
      # Mode de stockage : localfs | s3 | gs | hdfs
      BACKING_FS: ${BACKING_FS:-localfs}
      # Répertoire racine pour le stockage local (utilisé si BACKING_FS=localfs)
      LOCAL_FS_DIRECTORY: ${MOVETODATA_MOUNT_PATH:-/movetodata}
      JAVA_OPTS: >-
        -Xms512m
        -Xmx2g
        -XX:+UseG1GC
        -XX:MaxGCPauseMillis=200
        -Djava.security.egd=file:/dev/./urandom
    volumes:
      - ${MOVETODATA_MOUNT_PATH:-/movetodata}:${MOVETODATA_MOUNT_PATH:-/movetodata}
      - /etc/movetodata:/etc/movetodata:ro
    ports:
      - "8080:8080"
    restart: on-failure
    healthcheck:
      # Accepte toute réponse HTTP (200 ou 401) — le serveur est vivant s'il répond
      test: ["CMD-SHELL", "curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/endpoints/health | grep -qE '^[2-5]' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    networks:
      - movetodata-net
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "10"

  frontend:
    image: movetodata/frontend:latest
    container_name: movetodata-frontend
    build:
      context: ../frontend
      dockerfile: Dockerfile
      args:
        REACT_APP_API_URL: ${BASE_URL:-http://localhost:8080}/api
    depends_on:
      boson:
        condition: service_healthy
    volumes:
      - ${MOVETODATA_MOUNT_PATH:-/movetodata}/frontend/build:/app
      - ${MOVETODATA_MOUNT_PATH:-/movetodata}/frontend/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8081:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:80 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - movetodata-net
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "5"

  movetodata-docs:
    image: movetodata/docs:latest
    container_name: movetodata-docs
    build:
      context: ../movetodata-docs
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - movetodata-net
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"

networks:
  movetodata-net:
    name: movetodata-network
    driver: bridge
