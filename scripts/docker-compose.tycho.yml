# =============================================================================
# MoveToData Platform — Stack Tycho (Apache Superset BI)
# Usage : docker compose -f docker-compose.tycho.yml --env-file .env.movetodata up -d
# =============================================================================
version: "3.8"

x-tycho-image: &tycho-image movetodata/tycho:latest
x-tycho-user:  &tycho-user  root

x-tycho-depends: &tycho-depends
  - tycho-db
  - movetodata-redis

x-tycho-volumes: &tycho-volumes
  - ../tycho/docker:/app/docker:ro
  - ${MOVETODATA_MOUNT_PATH:-/movetodata}/tycho/superset_home:/app/superset_home

services:

  # ---------------------------------------------------------------------------
  # Base de données PostgreSQL 14 — Tycho / Superset
  # ---------------------------------------------------------------------------
  tycho-db:
    image: postgres:14-alpine
    container_name: movetodata-tycho-db
    environment:
      POSTGRES_DB: ${TYCHO_DB_NAME:-superset}
      POSTGRES_USER: ${TYCHO_DB_USERNAME:-superset}
      POSTGRES_PASSWORD: ${TYCHO_DB_PASSWORD}
    volumes:
      - ${MOVETODATA_MOUNT_PATH:-/movetodata}/postgres/tycho:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5433:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${TYCHO_DB_USERNAME:-superset}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - movetodata-tycho-net
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  # ---------------------------------------------------------------------------
  # Redis — partagé entre Tycho (Celery) et le réseau MoveToData
  # (Si vous utilisez déjà movetodata-redis dans core, décommentez external)
  # ---------------------------------------------------------------------------
  movetodata-redis:
    image: redis:7-alpine
    container_name: movetodata-redis
    command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - ${MOVETODATA_MOUNT_PATH:-/movetodata}/redis:/data
    ports:
      - "127.0.0.1:6379:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - movetodata-tycho-net
      - movetodata-network

  # ---------------------------------------------------------------------------
  # Tycho App — Superset application principale
  # ---------------------------------------------------------------------------
  tycho:
    image: *tycho-image
    container_name: movetodata-tycho
    build:
      context: ../tycho
      dockerfile: Dockerfile
    command: ["/app/docker/docker-bootstrap.sh", "app"]
    user: *tycho-user
    depends_on: *tycho-depends
    environment:
      DATABASE_HOST: tycho-db
      DATABASE_PORT: 5432
      DATABASE_DB: ${TYCHO_DB_NAME:-superset}
      DATABASE_USER: ${TYCHO_DB_USERNAME:-superset}
      DATABASE_PASSWORD: ${TYCHO_DB_PASSWORD}
      DATABASE_DIALECT: postgresql
      POSTGRES_DB: ${TYCHO_DB_NAME:-superset}
      POSTGRES_USER: ${TYCHO_DB_USERNAME:-superset}
      POSTGRES_PASSWORD: ${TYCHO_DB_PASSWORD}
      REDIS_HOST: movetodata-redis
      REDIS_PORT: ${REDIS_PORT:-6379}
      FLASK_ENV: ${FLASK_ENV:-production}
      SUPERSET_ENV: ${SUPERSET_ENV:-production}
      SUPERSET_LOAD_EXAMPLES: ${SUPERSET_LOAD_EXAMPLES:-no}
      SECRET_KEY: ${SUPERSET_SECRET_KEY}
      SUPERSET_PORT: ${SUPERSET_PORT:-8088}
      PYTHONPATH: /app/pythonpath:/app/docker/pythonpath_dev
    volumes: *tycho-volumes
    ports:
      - "8088:8088"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8088/health || exit 1"]
      interval: 30s
      timeout: 15s
      retries: 5
      start_period: 120s
    networks:
      - movetodata-tycho-net
      - movetodata-network
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "10"

  # ---------------------------------------------------------------------------
  # Tycho Init — Initialisation DB + admin user (one-shot)
  # ---------------------------------------------------------------------------
  tycho-init:
    image: *tycho-image
    container_name: movetodata-tycho-init
    command: ["/app/docker/docker-init.sh"]
    user: *tycho-user
    depends_on: *tycho-depends
    environment:
      DATABASE_HOST: tycho-db
      DATABASE_PORT: 5432
      DATABASE_DB: ${TYCHO_DB_NAME:-superset}
      DATABASE_USER: ${TYCHO_DB_USERNAME:-superset}
      DATABASE_PASSWORD: ${TYCHO_DB_PASSWORD}
      DATABASE_DIALECT: postgresql
      POSTGRES_DB: ${TYCHO_DB_NAME:-superset}
      POSTGRES_USER: ${TYCHO_DB_USERNAME:-superset}
      POSTGRES_PASSWORD: ${TYCHO_DB_PASSWORD}
      REDIS_HOST: movetodata-redis
      REDIS_PORT: ${REDIS_PORT:-6379}
      FLASK_ENV: ${FLASK_ENV:-production}
      SUPERSET_ENV: ${SUPERSET_ENV:-production}
      SUPERSET_LOAD_EXAMPLES: ${SUPERSET_LOAD_EXAMPLES:-no}
      SECRET_KEY: ${SUPERSET_SECRET_KEY}
      ADMIN_PASSWORD: ${TYCHO_ADMIN_PASSWORD:-admin}
    volumes: *tycho-volumes
    networks:
      - movetodata-tycho-net

  # ---------------------------------------------------------------------------
  # Tycho Worker — Celery worker (tâches asynchrones)
  # ---------------------------------------------------------------------------
  tycho-worker:
    image: *tycho-image
    container_name: movetodata-tycho-worker
    command: ["/app/docker/docker-bootstrap.sh", "worker"]
    user: *tycho-user
    depends_on: *tycho-depends
    environment:
      DATABASE_HOST: tycho-db
      DATABASE_PORT: 5432
      DATABASE_DB: ${TYCHO_DB_NAME:-superset}
      DATABASE_USER: ${TYCHO_DB_USERNAME:-superset}
      DATABASE_PASSWORD: ${TYCHO_DB_PASSWORD}
      REDIS_HOST: movetodata-redis
      REDIS_PORT: ${REDIS_PORT:-6379}
      FLASK_ENV: ${FLASK_ENV:-production}
      SECRET_KEY: ${SUPERSET_SECRET_KEY}
      PYTHONPATH: /app/pythonpath:/app/docker/pythonpath_dev
    volumes: *tycho-volumes
    restart: unless-stopped
    networks:
      - movetodata-tycho-net
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  # ---------------------------------------------------------------------------
  # Tycho Beat — Celery beat scheduler (tâches périodiques)
  # ---------------------------------------------------------------------------
  tycho-beat:
    image: *tycho-image
    container_name: movetodata-tycho-beat
    command: ["/app/docker/docker-bootstrap.sh", "beat"]
    user: *tycho-user
    depends_on: *tycho-depends
    environment:
      DATABASE_HOST: tycho-db
      DATABASE_DB: ${TYCHO_DB_NAME:-superset}
      DATABASE_USER: ${TYCHO_DB_USERNAME:-superset}
      DATABASE_PASSWORD: ${TYCHO_DB_PASSWORD}
      REDIS_HOST: movetodata-redis
      REDIS_PORT: ${REDIS_PORT:-6379}
      FLASK_ENV: ${FLASK_ENV:-production}
      SECRET_KEY: ${SUPERSET_SECRET_KEY}
      PYTHONPATH: /app/pythonpath:/app/docker/pythonpath_dev
    volumes: *tycho-volumes
    restart: unless-stopped
    networks:
      - movetodata-tycho-net
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"

  # ---------------------------------------------------------------------------
  # Tycho WebSocket — Temps réel
  # ---------------------------------------------------------------------------
  tycho-websocket:
    image: node:18-alpine
    container_name: movetodata-tycho-ws
    command: ["/app/docker/docker-frontend.sh"]
    working_dir: /app/superset-websocket
    depends_on:
      - movetodata-redis
    environment:
      PORT: 8090
      REDIS_HOST: movetodata-redis
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_SSL: "false"
    volumes:
      - ../tycho/superset-websocket:/app/superset-websocket
    ports:
      - "127.0.0.1:8090:8090"
    restart: unless-stopped
    networks:
      - movetodata-tycho-net
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"

networks:
  movetodata-tycho-net:
    name: movetodata-tycho-network
    driver: bridge
  movetodata-network:
    external: true
    name: movetodata-network
