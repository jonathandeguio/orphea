import { getLanguageLabel } from "utils/utilities";

export const SELECTION_TEXT = getLanguageLabel("selection");
export const KEEP_TEXT = getLanguageLabel("keep");
export const REMOVE_TEXT = getLanguageLabel("remove");
