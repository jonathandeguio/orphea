/**
 * Lets Start moving all bezier functions to this controller.
 * Inside a Hook.
 */

const useBezierController = () => {
  /**
   * ALL BUSINESS LOGIC.
   */
  return {};
};

export default useBezierController;
